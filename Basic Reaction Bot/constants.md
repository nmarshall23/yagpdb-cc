# Constants

```
{{ const := (sdict
    "dbPrefix" "test"
    "actionMsgkey" "test--actionMsgId"
    "emoji" (cslice "" "" "")
    "userValueDbPrefix" "test--user|"
    "userPattern" "test--user|%"
    "cc" (sdict
      "update" 0
      "reset" 0
    )
  )
}}
```
